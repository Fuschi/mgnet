% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2b_classes_group.R
\name{group_taxa}
\alias{group_taxa}
\title{Group Taxa in an mgnet Object}
\usage{
group_taxa(object, ...)
}
\arguments{
\item{object}{An \code{mgnet} object (or \code{mgnetList}).}

\item{...}{One or more unquoted expressions or column names from the taxa metadata.
Each specified column must exist in the taxa data.
\emph{To remove grouping, use \code{group_taxa(object, NULL)}.}}
}
\value{
The \code{mgnet} object (or \code{mgnetList}) with the new taxa grouping.
}
\description{
Groups taxa in an \code{mgnet} object by one or more specified variables from the taxa metadata.
Subsequent functions (e.g. \code{mutate_taxa}, \code{filter_taxa}, \code{mutate_netw}) will apply their operations
respecting these grouping variables.
}
\details{
\itemize{
\item If you pass multiple variables (e.g., \code{Genus}, \code{Species}), taxa are grouped by the combination of those variables.
\item If you pass exactly \code{NULL}, any existing taxa grouping is removed.
\item \strong{No Groups Specified}: If you do \emph{not} specify any groups \emph{and} your expressions do not reference abundance
variables (\code{abun}, \code{rela}, \code{norm}), the transformations use \strong{no grouping}. However, if your expressions \strong{do}
reference abundance variables, they automatically group by \code{sample_id} so that transformations apply per sample.
\item \strong{mgnetList Objects}: When grouping an \code{mgnetList}, the same logic applies to each \code{mgnet} inside the list.
}
}
