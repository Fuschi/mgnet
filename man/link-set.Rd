% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-setters.R
\name{link<-}
\alias{link<-}
\alias{link<-,mgnet-method}
\alias{link<-,mgnets-method}
\title{Update Network Links (Edges) for mgnet and mgnets Objects}
\usage{
link(object) <- value
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object to be updated.}

\item{value}{The new edge table(s): a data frame/tibble for \code{mgnet}, or
a named list of data frames / a single data frame with \code{mgnet} for \code{mgnets}.}
}
\value{
The updated \code{mgnet} or \code{mgnets} object with new network links.
}
\description{
This function sets the network links (edges) for \code{mgnet} objects and also
updates each \code{mgnet} inside an \code{mgnets}. For \code{mgnet}, the value must be a
data frame / tibble with at least columns \code{from} and \code{to}. For \code{mgnets},
the value can be either:
\itemize{
\item a \strong{named list} of data frames (one per contained \code{mgnet}, names matching \code{names(object)}), or
\item a \strong{single} data frame with a routing column \code{mgnet} (plus \code{from} and \code{to})
which is split and dispatched to each element.
}
}
\details{
The existing vertex set and graph directedness are preserved from the
current network. Edge endpoints must be a subset of the existing vertex
names; otherwise an error is raised.
}
