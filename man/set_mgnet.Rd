% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-set.R
\name{set_mgnet}
\alias{set_mgnet}
\alias{set_mgnet,mgnet-method}
\alias{set_mgnet,mgnets-method}
\title{Set slots of an \code{mgnet} object using tidy-style expressions}
\usage{
set_mgnet(object, ..., .cores, .packages = NULL)

\S4method{set_mgnet}{mgnet}(object, ..., .cores, .packages = NULL)

\S4method{set_mgnet}{mgnets}(object, ..., .cores, .packages = NULL)
}
\arguments{
\item{object}{An object of class \code{mgnet}.}

\item{...}{Named expressions, where each name must be one of the allowed
slot names: \code{abun}, \code{rela}, \code{norm}, \code{meta}, \code{taxa}, \code{netw}, \code{comm}.}

\item{.cores}{Integer number of CPU cores to use when \code{object} is an
\code{mgnets} object. If not supplied, the method for \code{mgnets} will use
\code{min(length(object), parallel::detectCores() - 1)} (with a safe
fallback to 1). For \code{mgnet} objects, \code{.cores} is ignored and computation
is always sequential.}

\item{.packages}{Optional character vector of package names to be loaded
on worker nodes when \code{.cores > 1}. This is useful when expressions
in \code{set_mgnet()} use functions not qualified with \code{pkg::fun}. Packages
must already be installed. Defaults include \code{"mgnet"} and \code{"magrittr"}.}
}
\value{
The modified \code{mgnet} object with updated slots.
}
\description{
\code{set_mgnet()} lets you compute and assign one or more slots of an \code{mgnet}
object using rlang-style expressions that can reference existing slots by
their bare names (e.g., \code{abun}, \code{rela}, \code{norm}, \code{meta}, \code{taxa}, \code{netw}, \code{comm}).
Expressions are evaluated \strong{sequentially} in the order provided, so later
expressions can depend on values assigned by earlier ones.
}
\section{How name resolution works}{

Within each expression, the symbols \code{abun}, \code{rela}, \code{norm}, \code{meta}, \code{taxa},
\code{netw}, and \code{comm} resolve to the \strong{current} working values (initially taken
from the object, then updated as each expression is assigned).
}

