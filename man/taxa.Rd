% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-getters.R
\name{taxa}
\alias{taxa}
\alias{taxa,mgnet-method}
\alias{taxa,mgnets-method}
\title{Get taxa-level information from \code{mgnet} and \code{mgnets} objects}
\usage{
taxa(object, .fmt = "df", .empty = "id", .collapse = FALSE)

\S4method{taxa}{mgnet}(object, .fmt = "df", .empty = "id", .collapse = FALSE)

\S4method{taxa}{mgnets}(object, .fmt = "df", .empty = "id", .collapse = FALSE)
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object.}

\item{.fmt}{Output format. One of \code{"df"} or \code{"tbl"}.}

\item{.empty}{What to return when no taxa-level information is available. One
of \code{"id"} or \code{"NULL"}.}

\item{.collapse}{Logical. For \code{mgnets}, whether to collapse the output into a
single table.}
}
\value{
For \code{mgnet}, a data.frame/tibble with taxa-level information.

For \code{mgnets}, either a named list of taxa-level tables or a single collapsed
table if \code{.collapse = TRUE}.
}
\description{
Retrieve taxa-level information stored in the \code{taxa} slot and, when present,
community assignments stored in the \code{comm} slot.

For a single \code{mgnet}, the result always includes \code{taxa_id}. When communities
are available, \code{comm_id} is also included.

For \code{mgnets}, collapsed output always includes both \code{mgnet} and \code{taxa_id}.
}
