% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-longer.R
\name{mgnet_longer}
\alias{mgnet_longer}
\alias{mgnet_longer,mgnet-method}
\alias{mgnet_longer,mgnets-method}
\title{Convert an \code{mgnet} or \code{mgnets} object to a long format table}
\usage{
mgnet_longer(object)

\S4method{mgnet_longer}{mgnet}(object)

\S4method{mgnet_longer}{mgnets}(object)
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object.}
}
\value{
A tibble in long format.

For \code{mgnet}, the tibble contains:
\itemize{
\item \code{sample_id}
\item \code{taxa_id}
\item optional abundance columns (\code{abun}, \code{rela}, \code{norm})
\item sample metadata from \code{meta(object)}
\item taxa-level information from \code{taxa(object)}
}

For \code{mgnets}, the tibble additionally contains:
\itemize{
\item \code{mgnet}: the name of the originating \code{mgnet} object
}
}
\description{
\code{mgnet_longer()} converts an \code{mgnet} object into a long-format tibble
containing all \code{sample_id} × \code{taxa_id} combinations, together with any
available abundance matrices (\code{abun}, \code{rela}, \code{norm}) and associated
metadata (e.g. \code{meta}, \code{taxa}, \code{comm_id}).

For \code{mgnets}, the method applies the same transformation to each contained
\code{mgnet} object and combines the results into a single tibble with an
additional \code{mgnet} column.
}
\details{
The output includes all sample–taxa combinations, even when abundance values
are missing, so abundance columns may contain \code{NA}.
}
