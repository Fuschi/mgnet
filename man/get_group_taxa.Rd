% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2b_classes_group.R
\name{get_group_taxa}
\alias{get_group_taxa}
\title{Get Taxa Grouping Variables from an mgnet Object}
\usage{
get_group_taxa(object)
}
\arguments{
\item{object}{An \code{mgnet} object or an \code{mgnetList}.}
}
\value{
\itemize{
\item For a single \code{mgnet} object, a character vector of grouping variable names,
or \code{NULL}/\code{character(0)} if no grouping is in effect.
\item For an \code{mgnetList}, a list of such character vectors (one per \code{mgnet}).
}
}
\description{
Retrieves the names of variables used for grouping taxa in an \code{mgnet} object.
These are set by \code{\link[=group_taxa]{group_taxa()}} and determine how subsequent functions
(e.g., \code{mutate_taxa}, \code{filter_taxa}, \code{mutate_netw}) operate on grouped taxa.
}
\details{
\itemize{
\item If this function returns \strong{\code{NULL}}, it indicates there is no explicit
grouping information. In that scenario, expressions that do \strong{not}
reference abundance variables (\code{abun}, \code{rela}, \code{norm}) proceed \strong{without} grouping;
expressions that \strong{do} reference those variables automatically group by
\code{sample_id}.
\item If this function returns an \strong{empty character vector} (i.e., \code{character(0)}),
it means grouping has been explicitly cleared with \code{group_taxa(object, NULL)}.
No grouping is enforced, even for abundance-related expressions.
\item If it returns a \strong{character vector} of column names, those columns are
used for grouping.
\item For an \code{mgnetList}, each individual \code{mgnet} in the list may have its own
grouping attributes. Typically, the \code{mgnet} identifier is always included
in grouped operations across the list context.
}
}
