% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-base-methods.R
\name{link_id}
\alias{link_id}
\alias{link_id,mgnet-method}
\alias{link_id,mgnets-method}
\title{Retrieve Link IDs}
\usage{
link_id(object, selected = TRUE)
}
\arguments{
\item{object}{An object of class \code{mgnet} or \code{mgnets}.}

\item{selected}{Logical. If \code{TRUE} (default), return link IDs from
the currently \strong{selected} subgraph (if any); if \code{FALSE}, return link IDs
from the full network.}
}
\value{
\itemize{
\item For an \code{mgnet}: an atmgnet vector (usually \code{character}) of link IDs taken
from the edge attribute \code{link_id} of \code{netw(object, selected = selected)}.
\item For an \code{mgnets}: a \strong{named list} with one element per contained \code{mgnet},
each element being the vector described above.
}
}
\description{
Return the \code{link_id} edge attribute from the network stored in an \code{mgnet}
object, or a named list of \code{link_id} vectors for each element of an \code{mgnets}.
}
\details{
This function is a light wrapper around \code{igraph::edge_attr()} applied to the
network returned by \code{netw(object, selected = selected)}. It assumes that the
network carries an edge attribute named \code{link_id}.
}
