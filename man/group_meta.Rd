% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2b_classes_group.R
\name{group_meta}
\alias{group_meta}
\title{Group Meta in an mgnet Object}
\usage{
group_meta(object, ...)
}
\arguments{
\item{object}{An \code{mgnet} object (or \code{mgnetList}).}

\item{...}{One or more unquoted expressions or column names from the meta data.
Each specified column must exist in the meta data.
\emph{To remove grouping, use \code{group_meta(object, NULL)}.}}
}
\value{
The \code{mgnet} object (or \code{mgnetList}) with the new meta grouping.
}
\description{
Groups the meta data in an \code{mgnet} object by one or more specified variables from the meta data.
Functions like \code{mutate_meta} and \code{filter_meta} will apply their operations based on these grouping variables.
}
\details{
\itemize{
\item If you pass multiple variables (e.g., \code{Site}, \code{Treatment}), the meta data are grouped by the combination of those variables.
\item If you pass exactly \code{NULL}, any existing meta grouping is removed.
\item \strong{No Groups Specified}: If you do \emph{not} specify any groups \emph{and} your expressions do not reference abundance
variables (\code{abun}, \code{rela}, \code{norm}), the transformations use \strong{no grouping}. However, if your expressions \strong{do}
reference abundance variables, they automatically group by \code{sample_id} so that transformations apply per sample.
\item \strong{mgnetList Objects}: When grouping an \code{mgnetList}, the same logic applies to each \code{mgnet} inside the list.
}
}
