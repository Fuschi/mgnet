% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-update.R
\name{update_mgnet}
\alias{update_mgnet}
\alias{update_mgnet,mgnet-method}
\alias{update_mgnet,mgnets-method}
\title{Update slots of an \code{mgnet} or \code{mgnets} object}
\usage{
update_mgnet(object, ..., .cores = 1L, .packages = NULL)

\S4method{update_mgnet}{mgnet}(object, ..., .cores = 1L, .packages = NULL)

\S4method{update_mgnet}{mgnets}(object, ..., .cores = 1L, .packages = NULL)
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object.}

\item{...}{Named expressions. The names indicate which target slot to update.}

\item{.cores}{Number of worker processes used for \code{mgnets}. Defaults to \code{1}.}

\item{.packages}{Optional character vector of package names to load on worker
processes when \code{.cores > 1}.}
}
\value{
An updated \code{mgnet} or \code{mgnets} object.
}
\description{
Evaluate one or more named expressions sequentially and use their results to
update slots of an \code{mgnet} object. Supported targets are:
\code{abun}, \code{rela}, \code{norm}, \code{meta}, \code{taxa}, \code{netw}, and \code{comm}.

Each expression is evaluated in a mask containing the current values of these
same targets. Updates are applied in order, so later expressions can depend
on earlier ones.

For \code{mgnets}, the same update is applied to each contained \code{mgnet} object,
optionally in parallel.
}
\details{
Expressions are evaluated sequentially. After each successful assignment, the
corresponding value is re-read from the updated object and inserted back into
the evaluation mask. This ensures that subsequent expressions see the actual
stored value after any setter-side reordering, coercion, or normalization.

Supported targets are:
\itemize{
\item \code{abun}
\item \code{rela}
\item \code{norm}
\item \code{meta}
\item \code{taxa}
\item \code{netw}
\item \code{comm}
}
}
