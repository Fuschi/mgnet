% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2b_classes_group.R
\name{get_group_meta}
\alias{get_group_meta}
\title{Get Meta Grouping Variables from an mgnet Object}
\usage{
get_group_meta(object)
}
\arguments{
\item{object}{An \code{mgnet} object or an \code{mgnetList}.}
}
\value{
\itemize{
\item For a single \code{mgnet}, a character vector of grouping variable names, or
\code{NULL}/\code{character(0)} if no grouping is in effect.
\item For an \code{mgnetList}, a list of such character vectors (one per \code{mgnet}).
}
}
\description{
Retrieves the names of variables used for grouping the meta data in an \code{mgnet} object.
These are set by \code{\link[=group_meta]{group_meta()}} and determine how subsequent functions
(e.g., \code{mutate_meta}, \code{filter_meta}) operate on grouped meta.
}
\details{
\itemize{
\item If this function returns \strong{\code{NULL}}, there is no explicit grouping information.
In that case, expressions that do \strong{not} reference abundance variables
(\code{abun}, \code{rela}, \code{norm}) have \strong{no grouping}, while expressions referencing them
default to grouping by \code{sample_id}.
\item If it returns an \strong{empty character vector} (i.e., \code{character(0)}),
grouping was explicitly removed via \code{group_meta(object, NULL)}. No grouping is
enforced, even for abundance-related expressions.
\item If it returns a \strong{character vector} of column names, those columns
are used for grouping meta data.
\item For an \code{mgnetList}, each \code{mgnet} may store its own grouping variables, and
combined operations typically account for each \code{mgnet} in the list.
}
}
