% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4b_classes_mutate_link.R
\name{mutate_link}
\alias{mutate_link}
\alias{mutate_link,mgnet-method}
\title{Mutate Link Data for an mgnet object}
\usage{
mutate_link(object, ...)
}
\arguments{
\item{object}{An \code{mgnet} object containing an igraph network (retrieved by
\code{netw(object)}) and taxon data (retrieved by \code{gather_taxa(object)}).}

\item{...}{\code{dplyr}-style mutate expressions. Inside these expressions, you
can call the local helpers described above.}
}
\value{
The original \code{mgnet} object, updated with new or modified edge attributes.
}
\description{
\verb{mutate_link,mgnet-method} provides a data-frame-like interface for mutating
edge-level attributes in an igraph network associated with an \code{mgnet} object.

It can utilize columns from the "from" and "to" nodes via local helpers:
\itemize{
\item \code{from(var)}: Pull the column \code{var} from the node on the "from" side.
\item \code{to(var)}: Pull the column \code{var} from the node on the "to" side.
\item \code{either(expr)}: For each edge, returns \code{TRUE} if \emph{either} side meets \code{expr}.
\item \code{both(expr)}: For each edge, returns \code{TRUE} if \emph{both} sides meet \code{expr}.
\item \code{neither(expr)}: For each edge, returns \code{TRUE} if \emph{neither} side meets \code{expr}.
\item \code{one(expr)}: Returns \code{TRUE} if \emph{exactly one} side satisfies \code{expr}.
}

After constructing (or modifying) columns, the new columns are attached as
edge attributes in the igraph object. The \code{mgnet} object is then returned
with an updated network.
}
