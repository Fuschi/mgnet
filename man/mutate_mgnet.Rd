% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-mutate-filter.R
\name{mutate_mgnet}
\alias{mutate_mgnet}
\alias{mutate_meta}
\alias{mutate_taxa}
\alias{mutate_meta,mgnet-method}
\alias{mutate_meta,mgnets-method}
\alias{mutate_taxa,mgnet-method}
\alias{mutate_taxa,mgnets-method}
\title{Mutate \code{meta} or \code{taxa} slots in \code{mgnet} / \code{mgnets}}
\usage{
mutate_meta(object, ..., .ungroup = FALSE)

\S4method{mutate_meta}{mgnet}(object, ..., .ungroup = FALSE)

\S4method{mutate_meta}{mgnets}(object, ..., .ungroup = FALSE)

mutate_taxa(object, ..., .ungroup = FALSE)

\S4method{mutate_taxa}{mgnet}(object, ..., .ungroup = FALSE)

\S4method{mutate_taxa}{mgnets}(object, ..., .ungroup = FALSE)
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object.}

\item{...}{One or more tidy-evaluated expressions (as in \code{dplyr::mutate()}).
Named expressions create/overwrite columns with the given name.}

\item{.ungroup}{Logical, default \code{FALSE}. If \code{TRUE}, remove the mgnet grouping
from the object at the end.}
}
\value{
An updated \code{mgnet} or \code{mgnets} object.
}
\description{
Adds or modifies columns in the sample metadata (\code{meta}) or taxa metadata
(\code{taxa}) of an \code{mgnet} object. For \code{mgnets}, the same operation is applied
to each contained \code{mgnet}.
}
\details{
User expressions are evaluated with tidy evaluation (like \code{dplyr::mutate()}).
Expressions may reference columns in the current target table (\code{meta} or \code{taxa}).
If abundance fields are referenced (\code{abun}, \code{rela}, \code{norm}), the relevant
long-format abundance table is automatically joined to provide the necessary
context.
\subsection{Grouping semantics}{

These functions respect the grouping state set by \code{group_mgnet()}.
Grouping variables are retrieved via \code{get_group_mgnet(object)} and used to
define the context for \code{dplyr::group_by()} during evaluation.
\itemize{
\item If the object is \strong{not grouped} (\code{get_group_mgnet(object)} is \code{NULL}),
no grouping is applied.
\item If an expression \strong{references abundance fields} (\code{abun}, \code{rela}, \code{norm})
and \strong{no explicit grouping} is set, a minimal grouping is injected:
\itemize{
\item for \code{mgnet}: by \code{sample_id} (when mutating \code{meta}) or by \code{taxa_id}
(when mutating \code{taxa});
\item for \code{mgnets}: by \code{mgnet} + the corresponding id.
}
\item If an expression \strong{does not reference abundance fields}, grouping variables
that belong to the \emph{other} slot are ignored.
}
}

\subsection{Cross-slot variables}{

If an expression references variables from the other slot, the expression
must also reference at least one abundance field (\code{abun}, \code{rela}, \code{norm}),
because cross-slot information is only available through abundance-aware joins.
}
}
