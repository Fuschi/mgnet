% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-getters.R
\name{abundance-getters}
\alias{abundance-getters}
\alias{abun}
\alias{abun,mgnet-method}
\alias{abun,mgnets-method}
\alias{rela}
\alias{rela,mgnet-method}
\alias{rela,mgnets-method}
\alias{norm}
\alias{norm,mgnet-method}
\alias{norm,mgnets-method}
\title{Retrieve abundance-like matrices from \code{mgnet} and \code{mgnets}}
\usage{
abun(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{abun}{mgnet}(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{abun}{mgnets}(object, .fmt = "mat", .var = NULL, .fun = sum)

rela(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{rela}{mgnet}(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{rela}{mgnets}(object, .fmt = "mat", .var = NULL, .fun = sum)

norm(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{norm}{mgnet}(object, .fmt = "mat", .var = NULL, .fun = sum)

\S4method{norm}{mgnets}(object, .fmt = "mat", .var = NULL, .fun = sum)
}
\arguments{
\item{object}{An \code{mgnet} or \code{mgnets} object.}

\item{.fmt}{Output format. One of:
\itemize{
\item \code{"mat"}: return a matrix
\item \code{"df"}: return a data.frame
\item \code{"tbl"}: return a tibble
}}

\item{.var}{Optional character string giving a taxa-level variable used for
grouping and aggregation. If omitted, the original matrix is returned.}

\item{.fun}{A function used to aggregate values within each sample and group.
Defaults to \code{sum}.}
}
\value{
For an \code{mgnet} object:
\itemize{
\item if \code{.var} is \code{NULL}, the original matrix in the requested format;
\item otherwise, an aggregated matrix/table with rows corresponding to
\code{sample_id} and columns corresponding to the levels of \code{.var}.
}

For an \code{mgnets} object:
\itemize{
\item a named list containing the corresponding result for each contained
\code{mgnet}.
}
}
\description{
Retrieve and optionally aggregate abundance-like matrices stored in an
\code{mgnet} or \code{mgnets} object.

These methods support:
\itemize{
\item raw abundance via \code{\link[=abun]{abun()}},
\item relative abundance via \code{\link[=rela]{rela()}},
\item normalized abundance via \code{\link[=norm]{norm()}}.
}

If \code{.var} is provided, values are aggregated according to a taxa-level
variable returned by \code{\link[=taxa_vars]{taxa_vars()}}. Aggregation is performed independently
within each sample.
}
\details{
The abundance-like matrices are assumed to have:
\itemize{
\item rows = \code{sample_id}
\item columns = \code{taxa_id}
}

When \code{.var} is provided, the matrix is converted to long format, joined with
taxa-level information, aggregated by \code{sample_id} and \code{.var}, and then
reshaped back to wide format.
}
