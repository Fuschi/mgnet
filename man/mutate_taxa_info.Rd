% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4b_classes_mutate_info.R
\name{mutate_taxa_info}
\alias{mutate_taxa_info}
\alias{mutate_sample_info,mgnet-method}
\title{Modify and Augment Taxa Metadata in \code{mgnet} Objects}
\usage{
mutate_taxa_info(object, ..., .by)
}
\arguments{
\item{object}{An \code{mgnet} object. This function targets the \code{taxa} slot, which contains
metadata for each taxon in the object.}

\item{...}{Dynamic expressions or functions to be applied to the metadata.
These expressions can modify metadata fields in the \code{taxa} slot.}

\item{.by}{A character vector specifying the columns to group the data by before applying
transformations. The default is \code{"taxa_id"}. Grouping ensures that transformations
are contextually applied within each subgroup defined by \code{.by}. The use of \code{"sample_id"}
as a grouping variable is strictly prohibited to maintain consistency.}
}
\value{
Returns the modified \code{mgnet} object with the transformed \code{meta} slot reflecting
the applied transformations. All other slots and structure within the \code{mgnet}
object remain unchanged.
}
\description{
This function dynamically manipulates the \code{taxa} slot within \code{mgnet} objects by applying
user-defined transformations to the taxa metadata. It leverages the \code{tidyverse} tools
such as \code{dplyr} to enable flexible data transformations.
}
